@page "/chess"
@using ChessMate.Models
@using ChessMate.Services
@using ChessMate.Utilities
@inject IChessBoard Board
@inject IStateService State
@inject IGameEngine GameEngine

<h3>Chess Game</h3>

<div class="chess-container">
    <!-- Chessboard Section -->
    <div class="chess-board">
        <table class="chessboard">
            <thead>
                <tr>
                    <th></th>
                    @for (char col = 'a'; col <= 'h'; col++)
                    {
                        <th>@col</th>
                    }
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @for (int row = 0; row < 8; row++)
                {
                    <tr>
                        <th>@(8 - row)</th>
                        @for (int col = 0; col < 8; col++)
                        {
                            var position = new Position(row, col);
                            var piece = Board.GetPieceAt(position);
                            var whiteAttackers = State.GetWhiteAttackers(position);
                            var blackAttackers = State.GetBlackAttackers(position);
                            var isUnderAttackByWhite = whiteAttackers.Any();
                            var isUnderAttackByBlack = blackAttackers.Any();

                            <td @onclick="() => SelectSquare(position)"
                                class="@GetSquareCssClass(position, isUnderAttackByWhite, isUnderAttackByBlack)">
                                @if (piece != null)
                                {
                                    <span class="piece">@piece.Representation</span>
                                }

                                @if (isUnderAttackByWhite || isUnderAttackByBlack)
                                {
                                    <div class="attack-overlay">
                                        @* Display attackers *@
                                        @if (isUnderAttackByWhite)
                                        {
                                            <div class="attackers white">
                                                @foreach (var attacker in whiteAttackers)
                                                {
                                                    <span class="attacker">@attacker.Representation (@attacker.GetType().Name)</span>

                                                    <br />
                                                }
                                            </div>
                                        }
                                        @if (isUnderAttackByBlack)
                                        {
                                            <div class="attackers black">
                                                @foreach (var attacker in blackAttackers)
                                                {
                                                    <span class="attacker">@attacker.Representation (@attacker.GetType().Name)</span>

                                                    <br />
                                                }
                                            </div>
                                        }
                                    </div>
                                }
                            </td>
                        }
                        <th>@(8 - row)</th>
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <th></th>
                    @for (char col = 'a'; col <= 'h'; col++)
                    {
                        <th>@col</th>
                    }
                    <th></th>
                </tr>
            </tfoot>
        </table>
    </div>

    <!-- Game Information Section -->
    <div class="game-info">
        <h3>Game Info</h3>
        <p><strong>Current Player:</strong> @State.CurrentPlayer</p>
        <p><strong>Check:</strong> @(State.IsCheck ? "Yes" : "No")</p>
        <p><strong>Checkmate:</strong> @(State.IsCheckmate ? "Yes" : "No")</p>
        <p><strong>En Passant Target:</strong> @(State.EnPassantTarget?.ToChessNotation() ?? "None")</p>
    </div>

    <!-- Move Log Section -->
    <div class="move-log">
        <h3>Move Log</h3>
        <ul>
            @foreach (var move in State.MoveLog)
            {
                <li>@move</li>
            }
        </ul>
    </div>

    <!-- Captured Pieces Section -->
    <div class="captured-pieces">
        <h3>Captured Pieces</h3>
        <div class="captures white-captures">
            <h4>White Captures:</h4>
            @foreach (var piece in State.CapturedPieces.Where(p => p.Color == "Black"))
            {
                <span class="captured-piece">@piece.Representation</span>
            }
        </div>
        <div class="captures black-captures">
            <h4>Black Captures:</h4>
            @foreach (var piece in State.CapturedPieces.Where(p => p.Color == "White"))
            {
                <span class="captured-piece">@piece.Representation</span>
            }
        </div>
    </div>
</div>

<!-- Error Message Display -->
@if (!string.IsNullOrEmpty(errorMessage))
{
    <p class="error-message">@errorMessage</p>
}

@code {
    private string? errorMessage;
    private Position? selectedPosition;

    /// <summary>
    /// Renders the attackers for a given color.
    /// </summary>
    private RenderFragment LoopAttackers(List<ChessPiece> attackers, string color) => builder =>
    {
        foreach (var attacker in attackers)
        {
            builder.OpenElement(0, "span");
            builder.AddAttribute(1, "class", "attacker");
            builder.AddContent(2, $"{attacker.Representation} ({attacker.GetType().Name})");
            builder.CloseElement();
            builder.AddContent(3, "<br />");
        }
    };

    /// <summary>
    /// Handles the selection of a chessboard square.
    /// </summary>
    private void SelectSquare(Position position)
    {
        if (selectedPosition == null)
        {
            // Select the first square
            var piece = Board.GetPieceAt(position);
            if (piece != null && piece.Color == State.CurrentPlayer)
            {
                selectedPosition = position;
            }
            else
            {
                errorMessage = "Please select one of your own pieces.";
            }
        }
        else
        {
            var from = selectedPosition.Value;
            var to = position;

            if (from.Equals(to))
            {
                // Deselect if the same square is clicked
                selectedPosition = null;
                errorMessage = null;
                return;
            }

            bool success = GameEngine.ProcessMove(from, to);
            if (!success)
            {
                errorMessage = "Invalid move. Please try again.";
            }
            else
            {
                errorMessage = null;
            }
            selectedPosition = null;
        }
    }

    /// <summary>
    /// Determines the CSS classes for each square based on position and attack status.
    /// </summary>
    private string GetSquareCssClass(Position position, bool underAttackByWhite, bool underAttackByBlack)
    {
        string baseClass = (position.Row + position.Col) % 2 == 0 ? "light" : "dark";
        string attackClass = "";

        if (underAttackByWhite && underAttackByBlack)
        {
            attackClass = " both-attacked";
        }
        else if (underAttackByWhite)
        {
            attackClass = " attacked-white";
        }
        else if (underAttackByBlack)
        {
            attackClass = " attacked-black";
        }

        if (selectedPosition.HasValue && selectedPosition.Value.Equals(position))
        {
            attackClass += " selected";
        }

        return $"{baseClass}{attackClass}";
    }
}
